import { hash } from "@/lib/hash";
export async function collectHackerNews(){const ids:number[]=await fetch("https://hacker-news.firebaseio.com/v0/newstories.json").then(r=>r.json());const recent=ids.slice(0,80);const items=await Promise.all(recent.map(id=>fetch(`https://hacker-news.firebaseio.com/v0/item/${id}.json`).then(r=>r.json())));return items.filter(Boolean).map((x:any)=>({sourceKey:"hackernews",externalId:String(x.id),sourceUrl:x.url||`https://news.ycombinator.com/item?id=${x.id}`,author:x.by||null,title:x.title||"Untitled",body:x.text||"",publishedAt:new Date(x.time*1000).toISOString(),contentHash:hash(`${x.title}|${x.text||""}|${x.url||x.id}`),rawPayload:x}))}
